import { Metadata } from "next/types";

export const metadata: Metadata = {
  title: "clar1k ⋅ projects",
};

export default function ProjectsPage() {
  return (
    <main className="bg-grid flex min-h-screen w-full justify-center bg-[#f8f9fa] py-12 md:py-24 lg:py-32 lg:pt-24"></main>
  );
}
